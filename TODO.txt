Priority 4: (1 days)
    authorization / admin (account screen)

Priority 5: (4 hours)
    documentation
    comment and organize API code

Priority 6: (3 hours)
    deployment

Priority 7: 
    fix issues in ISSUES.txt
    write test suite and conduct user testing

Priority 8: 
    alpha version 
        Create Recipe button and separate screen for adding recipe (public/private)
            40 char limit on recipe names
        sorting algorithm for recipes (rating system)
        compatible with dark mode

Priority 9:
    conduct testing on alpha


in api: 
python3 -m venv env
source env/bin/activate
pip install django
pip install djangoframework
python -m pip install Pillow
pip install django-filter
brew upgrade openssl
env LDFLAGS="-I/usr/local/opt/openssl/include -L/usr/local/opt/openssl/lib" pip install psycopg2
pg_ctl -D /usr/local/var/postgres start
python manage.py runserver

in homemade_beta:
psql homemade_db
pg-start ?
pg-stop ?

in ui:
npm install
yarn start

to stop:
API: ^C then deactive 
DB: pg-stop then \q 
UI: ^C 


Foods to Add: 
    - tomato

Diet to Remove: 
    - sustainable
