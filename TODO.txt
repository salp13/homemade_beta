Priority 1:
    specifiedItems query in HomeResultsScreen

Priority 2:
    item icon images and recipe images (backend and frontend)
    test unlisted_foods
    persist shopping list reorder

Priority 3:
    sort fridge items by days to expiration
    remove 'unlisted_foods' from rendering with all foods searching
    fix bugs

Priority 4:
    authorization / admin
    compatible for all iphone versions

Priority 5:
    clean up all code
    documentation

Priority 6: 
    deployment

Priority 7: 
    conduct user testing on beta
    alpha version 
        restructure profile page to have metrics up top with fridge count 
        toggle between Your Recipes and Saved Recipes
        Create Recipe button and separate screen for adding recipe (public/private)
        sorting algorithm for recipes (rating system)
    conduct user testing on alpha


in api: 
python3 -m venv env
source env/bin/activate
pip install django
pip install djangoframework
pip install django-filter
brew upgrade openssl
env LDFLAGS="-I/usr/local/opt/openssl/include -L/usr/local/opt/openssl/lib" pip install psycopg2
pg_ctl -D /usr/local/var/postgres start
python manage.py runserver

in homemade_beta:
psql homemade_db
pg-start ?
pg-stop ?

in ui:
npm install
yarn start

to stop:
API: ^C then deactive 
DB: pg-stop then \q 
UI: ^C 
