Priority 9 (4 days):
    fix bugs found in beta
    deploy final beta version
    distribute beta testing through testflight again

Priority 10 (7 days):
    alpha development found in ALPHA.txt
    expand beta testing

Priority 11 (7 day): 
    documentation
    comment and organize API code
    clean up UI
        follow logic and take out unnecessary code
        modularize api requests (url, request type, response, error)
        make function names more specific
        modularize asyncstorage functionality
        find common functions and take out
            save/unsave recipes
            make requests to api
            add item to the fridge (addscreen, shoppinglistscreen)
            add item to the shopping list (addscreen, individualscreen)
            filtering recipes (search and homeresults)

Priority 12 (3 day):
    figure out monetization 
    put on app store




in api: 
python3 -m venv env
source env/bin/activate
pip install django
pip install djangoframework
python -m pip install Pillow
pip install django-filter
pip install django-storages[azure]
brew upgrade openssl
env LDFLAGS="-I/usr/local/opt/openssl/include -L/usr/local/opt/openssl/lib" pip install psycopg2
pg_ctl -D /usr/local/var/postgres start
python manage.py runserver

in homemade_beta:
psql homemade_db
pg-start ?
pg-stop ?

in ui:
npm install
yarn start

to stop:
API: ^C then deactive 
DB: pg-stop then \q 
UI: ^C 


Foods to Add: 
    - tomato

Diet to Remove: 
    - sustainable
